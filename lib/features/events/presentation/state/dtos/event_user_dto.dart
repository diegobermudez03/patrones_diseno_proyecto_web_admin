class EventUserDto{
  final int occasionId;
  final String email;
  final String phone;
  final String stateName;
  final bool isInside;

  EventUserDto(this.occasionId, this.email, this.phone, this.stateName, this.isInside);
}